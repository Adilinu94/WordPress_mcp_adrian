# Secrets — never commit
mcp-server-config.json
local-xdebuginfo.php
.env
.env.local

# IDE
.idea/
.vscode/
*.code-workspace

# macOS
.DS_Store

# PHP tools (generated locally, not needed in dist)
.psalm-cache/

# Composer tool + deps (nie committen, via 'composer install' erzeugen)
composer.phar
vendor/
